# Production agent workflows and the reusable bounded runtime.

set(AGENTS_SOURCES
    dsl_grammar.hpp
    dsl_interpreter.hpp
    dsl_interpreter.cpp
    music_helpers.hpp
    internal_plugins.hpp
    compact_parser.hpp
    compact_parser.cpp
    conversation_store.hpp
    conversation_store.cpp
    agent_runtime.hpp
    agent_runtime.cpp
    fast_inference_policy.hpp
    fast_inference_policy.cpp
    console_agent_orchestrator.hpp
    console_agent_orchestrator.cpp
    instruction_executor.hpp
    instruction_executor.cpp
    llm_config_utils.hpp
    llm_presets.hpp
    llm_presets.cpp
    command_agent.hpp
    command_agent.cpp
    command_model.hpp
    command_model.cpp
    command_model_data.hpp
    command_model_data.cpp
    unigram_tokenizer.hpp
    unigram_tokenizer.cpp
    command_model_onnx.hpp
    command_model_onnx.cpp
    command_model_downloader.hpp
    command_model_downloader.cpp
    music_agent.hpp
    music_agent.cpp
    chord_agent.hpp
    chord_agent.cpp
    midi_context.hpp
    midi_context.cpp
    drummer_agent.hpp
    drummer_agent.cpp
    gain_staging_agent.hpp
    gain_staging_agent.cpp
    mixing_agent.hpp
    mixing_agent.cpp
    automation_parser.hpp
    automation_parser.cpp
    automation_executor.hpp
    automation_executor.cpp
    automation_agent.hpp
    automation_agent.cpp
    controller_profile_agent.hpp
    controller_profile_agent.cpp
    llama_model_manager.hpp
    llama_model_manager.cpp
    llama_local_client.hpp
    llama_local_client.cpp
    sunroom_mlx_client.cpp
    sunroom_mlx_client.hpp
    llm_client_factory.hpp
    model_downloader.hpp
    model_downloader.cpp
    four_osc_agent.hpp
    four_osc_agent.cpp
    theme_agent.cpp
    four_osc_apply.hpp
    four_osc_apply.cpp
    poly_step_sequencer_agent.hpp
    poly_step_sequencer_agent.cpp
    poly_step_sequencer_apply.hpp
    poly_step_sequencer_apply.cpp
    step_sequencer_agent.hpp
    step_sequencer_agent.cpp
    step_sequencer_apply.hpp
    step_sequencer_apply.cpp
    faust_agent.hpp
    faust_agent.cpp
    device_ai_agent.hpp
    sound_design_agent.hpp
    sound_design_agent.cpp
    generic_sound_design_agent.hpp
    generic_sound_design_agent.cpp
    coder_agent.hpp
    coder_agent.cpp
    mcp/MCPClient.hpp
    mcp/MCPClient.cpp
    mcp/MCPServerManager.hpp
    mcp/MCPServerManager.cpp
)

# Create the agents library
add_library(magda_agents STATIC ${AGENTS_SOURCES})

# command_model_data.cpp is ~51k weight literals generated by
# prototypes/command-model-poc/model/export_cpp.py. That generator now emits
# f-suffixed literals, so C4305 (truncation from double to float) should not
# fire at all; this keeps the Windows log readable if the file is ever
# regenerated by an older copy of the exporter, where the warning lands once
# per element and buries everything else.
if(MSVC)
    set_source_files_properties(command_model_data.cpp
        PROPERTIES COMPILE_OPTIONS "/wd4305")
endif()

# Link with JUCE for threading and utilities
target_link_libraries(magda_agents
    PUBLIC
    magda_daw
    PRIVATE
    Threads::Threads
    llama
    ggml
)

# Include directories
target_include_directories(magda_agents
    PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}
    PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/../daw
)

# Set target properties
set_target_properties(magda_agents PROPERTIES
    CXX_STANDARD 20
    CXX_STANDARD_REQUIRED ON
    CXX_EXTENSIONS OFF
)

target_compile_definitions(magda_agents PRIVATE SUNROOM_SOURCE_DIR="${PROJECT_SOURCE_DIR}")
if(APPLE)
    target_link_libraries(magda_agents PRIVATE "-framework Security")
endif()

# Compiler warnings
if(MSVC)
    target_compile_options(magda_agents PRIVATE /W4)
else()
    target_compile_options(magda_agents PRIVATE -Wall -Wextra -Wpedantic)
endif()

if(CMAKE_CXX_COMPILER_ID MATCHES "Clang|GNU")
    target_precompile_headers(magda_agents PRIVATE <juce_gui_basics/juce_gui_basics.h>)
endif()
