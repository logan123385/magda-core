# DAW Domain - Digital Audio Workstation Core + UI
# This file declares the magda_daw library, the magda_daw_app GUI target,
# and the magda_plugin_scanner helper. Per-directory CMakeLists.txt files
# under this tree contribute their own sources via target_sources().

# Generate version header from project version
configure_file(version.hpp.in "${CMAKE_CURRENT_BINARY_DIR}/generated/version.hpp" @ONLY)

# Safety net: detect git-lfs pointer files before juce_add_binary_data embeds
# them. Without this, a clone without git-lfs would build successfully but
# silently ship a ~130-byte pointer in place of the real font, and CJK text
# would render as tofu at runtime.
set(LFS_TRACKED_FILES
    "${CMAKE_CURRENT_SOURCE_DIR}/../../assets/fonts/NotoSansCJKsc-Regular.otf"
)
foreach(LFS_FILE ${LFS_TRACKED_FILES})
    if(EXISTS "${LFS_FILE}")
        file(READ "${LFS_FILE}" _lfs_head LIMIT 64)
        if(_lfs_head MATCHES "^version https://git-lfs\\.github\\.com/spec/")
            message(FATAL_ERROR
                "\n"
                "Git LFS file not fetched: ${LFS_FILE}\n"
                "This file is tracked by git-lfs but was checked out as a pointer.\n"
                "Install git-lfs and pull the real content:\n"
                "    git lfs install\n"
                "    git lfs pull\n")
        endif()
    endif()
endforeach()

# Create binary data for fonts and icons
juce_add_binary_data(MagdaAssets
    SOURCES
    # Fonts
    ../../assets/fonts/Inter-Regular.ttf
    ../../assets/fonts/Inter-Medium.ttf
    ../../assets/fonts/Inter-SemiBold.ttf
    ../../assets/fonts/Inter-Bold.ttf
    "../../assets/fonts/Microgramma D Extended Bold.otf"
    ../../assets/fonts/JetBrainsMono-Regular.ttf
    # CJK fallback — covers Simplified/Traditional Chinese, Japanese, Korean
    ../../assets/fonts/NotoSansCJKsc-Regular.otf
    # General UI icons
    ../../assets/icons/general/enter.svg
    ../../assets/icons/general/track.svg
    ../../assets/icons/general/clip.svg
    ../../assets/icons/general/copy.svg
    ../../assets/icons/general/copycontent.svg
    ../../assets/icons/general/delete.svg
    ../../assets/icons/general/random.svg
    ../../assets/icons/general/sidechain.svg
    ../../assets/icons/general/compare.svg
    ../../assets/icons/general/add.svg
    ../../assets/icons/general/settings_nobg.svg
    ../../assets/icons/general/menu.svg
    ../../assets/icons/general/funnel.svg
    ../../assets/icons/general/learn.svg
    ../../assets/icons/general/refresh.svg
    ../../assets/icons/general/send.svg
    ../../assets/icons/general/chevron_left.svg
    ../../assets/icons/general/chevron_right.svg
    ../../assets/icons/general/chevron_down.svg
    ../../assets/icons/general/bar_chart.svg
    ../../assets/icons/general/link_flat.svg
    ../../assets/icons/general/play_bare.svg
    ../../assets/icons/general/server_play.svg
    ../../assets/icons/general/server_stop.svg
    ../../assets/icons/general/open_in_new.svg
    ../../assets/icons/general/power.svg
    ../../assets/icons/general/toggle_off.svg
    ../../assets/icons/general/toggle_on.svg
    ../../assets/icons/general/input-output.svg
    ../../assets/icons/general/save.svg
    ../../assets/icons/general/folder-open.svg
    ../../assets/icons/general/piano_roll.svg
    ../../assets/icons/general/drum_grid.svg
    ../../assets/icons/general/faust-text-logo.svg
    ../../assets/icons/general/script.svg
    ../../assets/icons/general/code_blocks.svg
    ../../assets/icons/general/speaker.svg
    ../../assets/icons/general/speaker_simple.svg
    ../../assets/icons/general/master_on.svg
    ../../assets/icons/general/master_off.svg
    ../../assets/icons/general/chord.svg
    ../../assets/icons/general/solo.svg
    ../../assets/icons/general/track_record.svg
    ../../assets/icons/general/monitor_off.svg
    ../../assets/icons/general/monitor_on.svg
    ../../assets/BoldMGlyph.svg
    ../../assets/icons/general/headphones.svg
    ../../assets/icons/general/record-monitor.svg
    ../../assets/icons/general/metronome.svg
    ../../assets/icons/general/project_home.svg
    ../../assets/icons/general/harddrive.svg
    ../../assets/icons/general/database.svg
    ../../assets/icons/general/favorite.svg
    # MIDI editor icons
    ../../assets/icons/midi_editor/icon-velocity-boldm.svg
    ../../assets/icons/midi_editor/icon-cc-boldm.svg
    ../../assets/icons/midi_editor/icon-mpe-boldm.svg
    ../../assets/icons/midi_editor/icon-chord-boldm.svg
    ../../assets/icons/midi_editor/icon-fold-boldm.svg
    ../../assets/icons/midi_editor/lanes.svg
    ../../assets/icons/midi_editor/stacks.svg
    # Track chain icons
    ../../assets/icons/trackchain/icon-mods-boldm.svg
    ../../assets/icons/trackchain/icon-racks-boldm.svg
    ../../assets/icons/trackchain/icon-treeview-boldm.svg
    ../../assets/icons/trackchain/icon-presets-round-boldm.svg
    ../../assets/icons/trackchain/icon-sends-boldm.svg
    ../../assets/icons/trackchain/icon-inserts-boldm.svg
    ../../assets/icons/trackchain/oscilloscope3.svg
    ../../assets/icons/trackchain/icon-spectrum-boldm.svg
    ../../assets/icons/trackchain/icon-levels-boldm.svg
    ../../assets/icons/trackchain/icon-checkmix-boldm.svg
    ../../assets/icons/trackchain/devices/audio-device.svg
    ../../assets/icons/trackchain/devices/midi-device.svg
    ../../assets/icons/trackchain/devices/instrument-device.svg
    ../../assets/icons/trackchain/gain-staging.svg
    # Transport icons
    ../../assets/icons/transport/play.svg
    ../../assets/icons/transport/pause.svg
    ../../assets/icons/transport/stop.svg
    ../../assets/icons/transport/record.svg
    ../../assets/icons/transport/record_circle.svg
    ../../assets/icons/transport/loop.svg
    ../../assets/icons/transport/loop_icon.svg
    ../../assets/icons/transport/rewind.svg
    ../../assets/icons/transport/prev.svg
    ../../assets/icons/transport/next.svg
    ../../assets/icons/transport/punchin.svg
    ../../assets/icons/transport/punchout.svg
    ../../assets/icons/transport/automation_write.svg
    ../../assets/icons/transport/midi_qwerty.svg
    # Inspector icons
    ../../assets/icons/inspector/position.svg
    ../../assets/icons/inspector/fade_linear.svg
    ../../assets/icons/inspector/fade_convex.svg
    ../../assets/icons/inspector/fade_concave.svg
    ../../assets/icons/inspector/fade_scurve.svg
    ../../assets/icons/inspector/fade_gain.svg
    ../../assets/icons/inspector/fade_speedramp.svg
    # Resume/arrangement icons
    ../../assets/icons/resume/resume.svg
    # Automation icons
    ../../assets/icons/automation/horizontal_snap.svg
    ../../assets/icons/automation/vertical_snap.svg
    ../../assets/icons/automation/automation.svg
    ../../assets/icons/automation/automation_master_header.svg
    # Waveform icons
    ../../assets/icons/waveform/sine-wave.svg
    ../../assets/icons/waveform/time_bend.svg
    ../../assets/icons/waveform/note_slice.svg
    ../../assets/icons/waveform/curve.svg
    # Analysis device icons
    ../../assets/icons/analysis/postfx.svg
    ../../assets/icons/analysis/analysis2.svg
    # Toolbar icons
    ../../assets/icons/toolbar/density_small.svg
    ../../assets/icons/toolbar/density_medium.svg
    ../../assets/icons/toolbar/density_large.svg
    ../../assets/icons/toolbar/zoom_out_map.svg
    ../../assets/icons/toolbar/fit_width.svg
    ../../assets/icons/toolbar/fit_loop.svg
    ../../assets/icons/toolbar/horizontal.svg
    ../../assets/icons/toolbar/vertical.svg
    ../../assets/icons/toolbar/location.svg
    ../../assets/icons/toolbar/clock.svg
    # Footer view mode icons
    ../../assets/icons/footer/icon-session-boldm.svg
    ../../assets/icons/footer/icon-arrangement-boldm.svg
    ../../assets/icons/footer/icon-mix-boldm.svg
    # Legacy view mode icons (still used by the clip inspector view toggle)
    # Sidebar icons
    ../../assets/icons/sidebar/plug.svg
    # Browser icons
    ../../assets/icons/browser/browser.svg
    ../../assets/midi.svg
    ../../assets/icons/browser/icon-audio-boldm.svg
    ../../assets/icons/browser/icon-midi-boldm.svg
    ../../assets/icons/browser/icon-preset-boldm.svg
    ../../assets/icons/browser/icon-chordtrack-boldm.svg
    # Mixer fader icons
    ../../assets/icons/fader/fader_thumb.svg
    ../../assets/icons/fader/fader_track.svg
    # Mixer knob icons
    ../../assets/icons/knob/knob.svg
    ../../assets/icons/knob/knob_body.svg
    ../../assets/icons/knob/knob_pointer.svg
    # I/O routing icons
    ../../assets/icons/IO/Input.svg
    ../../assets/icons/IO/Output.svg
    ../../assets/icons/IO/multiout.svg
    # Panel open/close icons
    ../../assets/icons/panels/left_open.svg
    ../../assets/icons/panels/left_close.svg
    ../../assets/icons/panels/right_open.svg
    ../../assets/icons/panels/right_close.svg
    ../../assets/icons/panels/bottom_open.svg
    ../../assets/icons/panels/bottom_close.svg
    ../../assets/icons/panels/enter_fs.svg
    ../../assets/icons/panels/exit_fs.svg
    ../../assets/icons/panels/collapse.svg
    ../../assets/icons/panels/expand.svg
    # Panel tab icons (BoldM)
    ../../assets/icons/panels/icon-browser-boldm.svg
    ../../assets/icons/panels/icon-console-boldm.svg
    ../../assets/icons/panels/icon-inspector-boldm.svg
    ../../assets/icons/panels/icon-plugins-boldm.svg

    # Fontaudio waveform icons
    ../../assets/icons/fontaudio/fad-modsine.svg
    ../../assets/icons/fontaudio/fad-modtri.svg
    ../../assets/icons/fontaudio/fad-modsawup.svg
    ../../assets/icons/fontaudio/fad-modsquare.svg
    ../../assets/icons/fontaudio/fad-modrandom.svg
    # Fontaudio filter icons
    ../../assets/icons/fontaudio/fad-filter-bypass.svg
    ../../assets/icons/fontaudio/fad-filter-lowpass.svg
    ../../assets/icons/fontaudio/fad-filter-highpass.svg
    ../../assets/icons/fontaudio/fad-filter-bandpass.svg
    ../../assets/icons/fontaudio/fad-filter-notch.svg
    # Fontaudio logo icons
    ../../assets/icons/fontaudio/fad-logo-tracktion.svg
    ../../assets/icons/fontaudio/fad-logo-juce.svg
    # AI icons
    ../../assets/icons/ai/psychology.svg
    ../../assets/icons/ai/ai.svg
    # Session view icons
    ../../assets/icons/session/hide.svg
    ../../assets/icons/session/rate.svg
    # Cloud provider icons
    ../../assets/icons/cloud_providers/openai.svg
    ../../assets/icons/cloud_providers/anthropic.svg
    ../../assets/icons/cloud_providers/gemini.svg
    ../../assets/icons/cloud_providers/deepseek.svg
    ../../assets/icons/cloud_providers/openrouter.svg
    # App logo
    ../../assets/magda-lisa.svg
    ../../assets/conceptual-machines-badge.svg
    # Curated alias dictionaries (JSON path is reserved for third-party
    # plugin packs; MAGDA-internal plugin aliases live in
    # core/aliases/InternalPluginAliases.cpp).
    ../../assets/aliases/curated_index.json
    # Factory themes - embedded JSON palettes listed between the built-in
    # themes and the user's own files (see UserTheme.cpp).
    ../../assets/themes/concrete_warehouse.json
    ../../assets/themes/neon_cyberpunk.json
    ../../assets/themes/sunroom_sunset.json
)

# =============================================================================
# Targets
# =============================================================================

# magda_types: leaf library of JUCE-only value types shared by the DAW today and
# the device library later (epic #1836 / #1831). It borrows juce_core's headers
# and defines but does NOT link the module: the juce_core unity source is
# compiled once in magda_daw, so magda_types' undefined juce symbols resolve at
# the final link instead of duplicating the object. The value-type headers stay
# physically in core/ for now; relocating them into their own directory (and the
# include-boundary guard) lands with the device-library carve (#1834).
add_library(magda_types STATIC
    core/ParameterUtils.cpp
    # The shape of an automation lane, on its own, so the engine bakes a curve
    # through the same function the editor draws it with (#2118).
    core/AutomationCurve.cpp
    # The shape of a modulator's cycle, on the same terms: the editor's preview,
    # the fork's audio snapshot and the native engine's LFO read one curve
    # rather than three (#2119).
    core/ModCurve.cpp
    core/DeviceState.cpp
    # Clip placement rules (#2003). Pure, beat domain, no manager: they answer
    # against a lane of clips, which is what lets the arrangement ask about a
    # drag it has not committed and the native engine's clip snapshot ask with
    # no ClipManager at all (#2034). They live here rather than in magda_daw so
    # the engine links a leaf instead of the whole DAW.
    core/ClipOcclusion.cpp
    core/ClipFades.cpp
    # value-type headers (listed to document the leaf's surface)
    core/TypeIds.hpp
    core/ClipInfo.hpp
    core/ClipOcclusion.hpp
    core/ClipFades.hpp
    core/TechnicalText.hpp
    core/ChainNodePath.hpp
    core/ControlTarget.hpp
    core/ParameterInfo.hpp
    core/ParameterUtils.hpp
    core/AutomationCurve.hpp
    core/AutomationInfo.hpp
    core/ModCurve.hpp
    core/DeviceInfo.hpp
    core/DeviceState.hpp
    core/KitRow.hpp
    core/MacroInfo.hpp
    core/ModInfo.hpp
    core/TempoUtils.hpp
)
target_compile_features(magda_types PUBLIC cxx_std_20)
# juce_graphics as well as juce_core: ClipInfo names juce::Colour. Headers and
# defines only, on the same terms as juce_core above.
target_include_directories(magda_types
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}  # dependents resolve "core/<type>.hpp"
    PRIVATE $<TARGET_PROPERTY:juce::juce_core,INTERFACE_INCLUDE_DIRECTORIES>
            $<TARGET_PROPERTY:juce::juce_graphics,INTERFACE_INCLUDE_DIRECTORIES>)
target_compile_definitions(magda_types
    PRIVATE $<TARGET_PROPERTY:juce::juce_core,INTERFACE_COMPILE_DEFINITIONS>
            $<TARGET_PROPERTY:juce::juce_graphics,INTERFACE_COMPILE_DEFINITIONS>)

# Core library — populated by per-subdir CMakeLists.txt below.
add_library(magda_daw STATIC
    command.cpp
    magda.cpp
    command.hpp
    magda.hpp
)

# Frozen device SDK usage requirements and runtime — populated below and by
# audio/CMakeLists.txt. Device packs compile against magda::device_sdk; hosts
# link magda::devices.
add_library(magda_device_sdk INTERFACE)
add_library(magda::device_sdk ALIAS magda_device_sdk)
add_library(magda_devices STATIC)
add_library(magda::devices ALIAS magda_devices)
add_library(magda_base_devices STATIC)
add_library(magda::base_devices ALIAS magda_base_devices)
add_library(magda_tracktion_compat_devices STATIC)
set_target_properties(magda_devices PROPERTIES
    PUBLIC_HEADER
    "audio/plugins/DeviceParameterHandle.hpp;audio/plugins/DevicePluginHandle.hpp;audio/plugins/DeviceServices.hpp;audio/plugins/DeviceSessionKey.hpp;audio/plugins/IFaustEditorModel.hpp;audio/plugins/InternalPluginRegistry.hpp;audio/plugins/MagdaDevice.hpp"
)

# JUCE GUI app — sources contributed below via add_subdirectory().
juce_add_gui_app(magda_daw_app
    VERSION "${MAGDA_BUNDLE_VERSION}"
    COMPANY_NAME "Conceptual Machines"
    PRODUCT_NAME "SUNROOM"
    # Give the personal fork its own stable identity and macOS permissions.
    BUNDLE_ID "com.loganchambers.sunroom"
    ICON_BIG "${CMAKE_CURRENT_SOURCE_DIR}/../../assets/sunroom_icon.png"
    ICON_SMALL "${CMAKE_CURRENT_SOURCE_DIR}/../../assets/sunroom_icon.png"
    MICROPHONE_PERMISSION_ENABLED TRUE
    MICROPHONE_PERMISSION_TEXT "SUNROOM needs microphone access to record audio."
    PLIST_TO_MERGE
    "<plist version=\"1.0\"><dict>
        <key>NSDocumentsFolderUsageDescription</key>
        <string>SUNROOM needs access to your Documents folder to load and save projects.</string>
        <key>NSDesktopFolderUsageDescription</key>
        <string>SUNROOM needs access to your Desktop to load and save projects.</string>
        <key>NSDownloadsFolderUsageDescription</key>
        <string>SUNROOM needs access to your Downloads folder to import audio files.</string>
        <key>NSRemovableVolumesUsageDescription</key>
        <string>SUNROOM needs access to external drives to load and save projects and audio files.</string>
        <key>CFBundleDocumentTypes</key>
        <array>
            <dict>
                <key>CFBundleTypeName</key>
                <string>MAGDA Project</string>
                <key>CFBundleTypeRole</key>
                <string>Editor</string>
                <key>LSHandlerRank</key>
                <string>Alternate</string>
                <key>CFBundleTypeExtensions</key>
                <array>
                    <string>mgd</string>
                </array>
                <key>CFBundleTypeIconFile</key>
                <string>mgd_doc_icon</string>
            </dict>
        </array>
    </dict></plist>"
)

target_sources(magda_daw_app PRIVATE
    magda_daw_main.cpp
    sunroom/SunroomStudio.cpp
    sunroom/SunroomActions.cpp
)
target_compile_definitions(magda_daw_app PRIVATE SUNROOM_SOURCE_DIR="${PROJECT_SOURCE_DIR}")
if(APPLE)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
        "${PROJECT_SOURCE_DIR}/resources/sunroom"
        "$<TARGET_BUNDLE_DIR:magda_daw_app>/Contents/Resources/Sunroom")
else()
    # Non-Apple packages look for Sunroom next to the executable.
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
        "${PROJECT_SOURCE_DIR}/resources/sunroom"
        "$<TARGET_FILE_DIR:magda_daw_app>/Sunroom"
        COMMENT "Copying SUNROOM resources beside the executable")
    install(DIRECTORY "${PROJECT_SOURCE_DIR}/resources/sunroom/"
            DESTINATION bin/Sunroom)
endif()

# Bundle the .mgd document icon into the app's Resources so Finder shows it for
# project files (referenced by CFBundleTypeIconFile in the plist above).
if(APPLE)
    set_source_files_properties(
        "${CMAKE_CURRENT_SOURCE_DIR}/../../assets/mgd_doc_icon.icns"
        PROPERTIES MACOSX_PACKAGE_LOCATION Resources)
    target_sources(magda_daw_app PRIVATE
        "${CMAKE_CURRENT_SOURCE_DIR}/../../assets/mgd_doc_icon.icns")
endif()

# Out-of-process plugin scanner.
juce_add_console_app(magda_plugin_scanner
    VERSION "${MAGDA_BUNDLE_VERSION}"
    COMPANY_NAME "Conceptual Machines"
    PRODUCT_NAME "MAGDA Plugin Scanner"
    # Pin the bundle identifier so the COMPANY_NAME rename does not change it
    # (see magda_daw_app above). Keep the original com.MAGDA.MAGDAPluginScanner.
    BUNDLE_ID "com.MAGDA.MAGDAPluginScanner"
)

target_sources(magda_plugin_scanner PRIVATE
    engine/plugin_scanner_main.cpp
)

# Headless CLI for e2e project round-trips.
juce_add_console_app(magda_cli
    VERSION "${MAGDA_BUNDLE_VERSION}"
    COMPANY_NAME "Conceptual Machines"
    PRODUCT_NAME "MAGDA CLI"
    BUNDLE_ID "com.MAGDA.MAGDACLI"
)

# =============================================================================
# Per-subdirectory sources
# =============================================================================

add_subdirectory(cli)
add_subdirectory(core)
add_subdirectory(engine)
add_subdirectory(project)
add_subdirectory(music)
add_subdirectory(audio)
add_subdirectory(api)
add_subdirectory(interfaces)
add_subdirectory(profiling)
add_subdirectory(ui)
add_subdirectory(media_db)
add_subdirectory(transcription)
add_subdirectory(stem_separation)

# Keep the library boundary one-way: agents may link magda_daw, but no source
# compiled into magda_daw may include an agent header. This is a best-effort text
# scan of listed sources, not a transitive dependency-graph check. App-only UI
# sources are intentionally outside it because they form the composition layer.
get_target_property(_magda_daw_sources magda_daw SOURCES)
foreach(_magda_daw_source IN LISTS _magda_daw_sources)
    if(_magda_daw_source MATCHES "^\\$<")
        continue()
    endif()
    if(IS_ABSOLUTE "${_magda_daw_source}")
        set(_magda_daw_source_path "${_magda_daw_source}")
    else()
        set(_magda_daw_source_path "${CMAKE_CURRENT_SOURCE_DIR}/${_magda_daw_source}")
    endif()
    if(EXISTS "${_magda_daw_source_path}"
       AND _magda_daw_source_path MATCHES "\\.(c|cc|cpp|cxx|h|hh|hpp|hxx)$")
        file(READ "${_magda_daw_source_path}" _magda_daw_source_text)
        if(_magda_daw_source_text
           MATCHES "(^|\n)[ \t]*#[ \t]*include[ \t]*[<\"](\\.\\./)*agents/")
            message(FATAL_ERROR
                "magda_daw source includes an agent header: ${_magda_daw_source_path}")
        endif()

        # Core may query the opaque string-keyed registry seam, but it must
        # never depend on a concrete device implementation. Device packs own
        # those includes and contribute their specs through registerDevices().
        if(_magda_daw_source_path MATCHES "/core/")
            string(REGEX MATCHALL
                "#[ \t]*include[ \t]*[<\"][^>\"]*audio/plugins/[^>\"]*Plugin\\.hpp"
                _core_plugin_includes "${_magda_daw_source_text}")
            if(_core_plugin_includes)
                message(FATAL_ERROR
                    "core source includes a concrete device header: "
                    "${_magda_daw_source_path}\n${_core_plugin_includes}")
            endif()
        endif()
    endif()
endforeach()
unset(_core_plugin_include)
unset(_core_plugin_includes)
unset(_magda_daw_source_text)
unset(_magda_daw_source_path)
unset(_magda_daw_source)
unset(_magda_daw_sources)

# =============================================================================
# Linking, compile defs, includes
# =============================================================================

# Auto-detect Steinberg ASIO SDK for Windows ASIO support
if(WIN32)
    find_path(ASIO_SDK_DIR
        NAMES "common/iasiodrv.h"
        PATHS
            "${CMAKE_SOURCE_DIR}/third_party/asiosdk"
            "${CMAKE_SOURCE_DIR}/third_party/asio_sdk"
            "$ENV{ASIO_SDK_DIR}"
        DOC "Path to Steinberg ASIO SDK"
    )
    if(ASIO_SDK_DIR)
        message(STATUS "ASIO SDK found: ${ASIO_SDK_DIR}")
        set(JUCE_ASIO_ENABLED 1)
    else()
        message(STATUS "ASIO SDK not found - ASIO support disabled. "
            "Download from https://www.steinberg.net/asiosdk and place in third_party/asiosdk/")
        set(JUCE_ASIO_ENABLED 0)
    endif()
endif()

# JUCE / Tracktion modules are JUCE "module" INTERFACE libraries: their unity
# .cpp lives in INTERFACE_SOURCES, so linking them PUBLIC propagates those
# sources to every dependent, which then recompiles the entire JUCE+TE stack
# (measured: juce_core x14, juce_audio_basics x7 across the 7 targets). Compile
# them ONCE here instead:
#   - PRIVATE so the sources land only in magda_daw and do not propagate;
#   - as a STATIC lib, magda_daw still forwards the modules' LINK requirements
#     (system frameworks, the compiled objects) to final executables, made
#     explicit via the INTERFACE $<LINK_ONLY:...> entry;
#   - the compile usage (header search paths + JUCE_MODULE_AVAILABLE_* defines)
#     is re-exported below without the sources, so dependents still #include
#     JUCE headers but never recompile the modules.
set(MAGDA_JUCE_STACK
    juce::juce_core
    juce::juce_cryptography  # SHA-256 verification of downloaded model files
    juce::juce_data_structures
    juce::juce_audio_basics
    juce::juce_audio_devices
    juce::juce_audio_formats
    juce::juce_audio_utils
    juce::juce_gui_basics
    juce::juce_gui_extra
    juce::juce_osc  # OSC control surfaces (#1757); ships with JUCE, no new dependency
    tracktion::tracktion_engine
    juce_llm
)
set(MAGDA_DEVICE_JUCE_STACK
    juce::juce_core
    juce::juce_data_structures
    juce::juce_audio_basics
    juce::juce_audio_formats
    juce::juce_audio_processors
    juce::juce_dsp
)
target_link_libraries(magda_daw
    PUBLIC
    magda_types  # leaf value-type library (#1831); its types appear in magda_daw's public headers
    magda_music  # leaf music-theory library (#1833)
    "$<LINK_LIBRARY:WHOLE_ARCHIVE,magda_devices>"  # SDK runtime
    "$<LINK_LIBRARY:WHOLE_ARCHIVE,magda_base_devices>"  # engine-neutral base pack
    # Host-owned legacy devices retain registration anchors while their DSP
    # implementations migrate to MagdaDevice.
    "$<LINK_LIBRARY:WHOLE_ARCHIVE,magda_tracktion_compat_devices>"
    MagdaAssets
    PRIVATE
    ${MAGDA_JUCE_STACK}
    LibXml2::LibXml2
    # Remote API transport (#1856). PRIVATE on purpose: httplib.h appears in
    # remote_websocket_server.cpp and nowhere else, so no dependent inherits a
    # 20k-line header it does not use.
    httplib::httplib
    $<$<PLATFORM_ID:Linux>:juce::pkgconfig_JUCE_BROWSER_LINUX_DEPS>
    $<$<PLATFORM_ID:Linux>:juce::pkgconfig_JUCE_CURL_LINUX_DEPS>
    INTERFACE
    # Carry the modules forward to dependents for LINKING only (frameworks +
    # the objects already inside magda_daw's archive), never for compiling.
    $<LINK_ONLY:${MAGDA_JUCE_STACK}>
    $<$<PLATFORM_ID:Linux>:$<LINK_ONLY:juce::pkgconfig_JUCE_BROWSER_LINUX_DEPS>>
    $<$<PLATFORM_ID:Linux>:$<LINK_ONLY:juce::pkgconfig_JUCE_CURL_LINUX_DEPS>>
)

# The frozen SDK target carries only compile usage and link requirements. It
# deliberately does not own concrete device sources, so in-tree and private
# packs build under the same contract.
target_compile_features(magda_device_sdk INTERFACE cxx_std_20)
target_include_directories(magda_device_sdk
    INTERFACE
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/audio
)
foreach(_magda_device_module IN LISTS MAGDA_DEVICE_JUCE_STACK)
    target_include_directories(magda_device_sdk INTERFACE
        $<TARGET_PROPERTY:${_magda_device_module},INTERFACE_INCLUDE_DIRECTORIES>)
    target_compile_definitions(magda_device_sdk INTERFACE
        $<TARGET_PROPERTY:${_magda_device_module},INTERFACE_COMPILE_DEFINITIONS>)
endforeach()
target_link_libraries(magda_device_sdk
    INTERFACE
    magda_types
    magda_music
    $<LINK_ONLY:${MAGDA_DEVICE_JUCE_STACK}>
)

# magda_devices owns the SDK runtime. The base pack is a separate static
# library compiled against magda::device_sdk, exactly like an out-of-tree pack.
# WHOLE_ARCHIVE above retains its public-hook registration anchor.
target_compile_features(magda_devices PUBLIC cxx_std_20)
target_include_directories(magda_devices PRIVATE
    ${CMAKE_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/faust/architecture
)
target_link_libraries(magda_devices
    PUBLIC
    magda_device_sdk
    PRIVATE
    MagdaAssets
    magda::mutable
    faust::libfaust
)
target_compile_features(magda_base_devices PRIVATE cxx_std_20)
target_include_directories(magda_base_devices PRIVATE
    ${CMAKE_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/faust/architecture
)
target_link_libraries(magda_base_devices
    PRIVATE
    magda_device_sdk
    MagdaAssets
    faust::libfaust
)
target_compile_features(magda_tracktion_compat_devices PRIVATE cxx_std_20)
target_include_directories(magda_tracktion_compat_devices PRIVATE
    ${CMAKE_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/faust/architecture
    $<TARGET_PROPERTY:tracktion::tracktion_engine,INTERFACE_INCLUDE_DIRECTORIES>
)
target_compile_definitions(magda_tracktion_compat_devices PRIVATE
    $<TARGET_PROPERTY:tracktion::tracktion_engine,INTERFACE_COMPILE_DEFINITIONS>
)
target_link_libraries(magda_tracktion_compat_devices
    PRIVATE
    magda_device_sdk
    MagdaAssets
    magda::mutable
    faust::libfaust
)

# magda_faust_backend carries which libfaust factory this build actually
# contains, so FaustBackend.hpp resolves to the backend that is really linked.
target_link_libraries(magda_devices PRIVATE magda_faust_backend)
target_link_libraries(magda_base_devices PRIVATE magda_faust_backend)
target_link_libraries(magda_tracktion_compat_devices PRIVATE magda_faust_backend)

if(MAGDA_PRO_DEVICES)
    if(NOT EXISTS "${MAGDA_PRO_DEVICES_DIR}/CMakeLists.txt")
        message(FATAL_ERROR
            "MAGDA_PRO_DEVICES=ON but no device pack was found at "
            "${MAGDA_PRO_DEVICES_DIR}")
    endif()

    add_subdirectory(
        "${MAGDA_PRO_DEVICES_DIR}"
        "${CMAKE_BINARY_DIR}/magda_pro_devices"
    )
    if(NOT TARGET magda_pro_devices)
        message(FATAL_ERROR
            "The pro-device pack must define a magda_pro_devices target")
    endif()

    target_link_libraries(magda_daw
        PUBLIC "$<LINK_LIBRARY:WHOLE_ARCHIVE,magda_pro_devices>")
    target_compile_definitions(magda_daw PUBLIC MAGDA_PRO_DEVICES_ENABLED=1)
endif()

# Signalsmith's FFT-heavy processing cannot reliably meet real-time audio
# deadlines at -O0. Keep the rest of Debug builds fully debuggable, but compile
# Tracktion's dedicated time-stretch translation unit with optimisation so the
# local app exercises representative DSP performance.
set_source_files_properties(
    "${CMAKE_SOURCE_DIR}/third_party/tracktion_engine/modules/tracktion_engine/tracktion_engine_timestretch.cpp"
    TARGET_DIRECTORY magda_daw
    PROPERTIES COMPILE_OPTIONS
        "$<$<AND:$<CONFIG:Debug>,$<CXX_COMPILER_ID:MSVC>>:/O2>;$<$<AND:$<CONFIG:Debug>,$<NOT:$<CXX_COMPILER_ID:MSVC>>>:-O2>"
)

# Re-export JUCE/TE compile usage (include dirs + defines, NO sources) so
# dependents can still #include JUCE headers. magda_daw's own resolved
# INCLUDE_DIRECTORIES / COMPILE_DEFINITIONS already hold the full transitive
# JUCE closure (CMake resolved it when the modules were linked PRIVATE above),
# so forward exactly those - this avoids having to enumerate TE's hidden juce
# module dependencies by hand.
target_include_directories(magda_daw INTERFACE
    $<BUILD_INTERFACE:$<TARGET_PROPERTY:magda_daw,INCLUDE_DIRECTORIES>>)
target_compile_definitions(magda_daw INTERFACE
    $<TARGET_PROPERTY:magda_daw,COMPILE_DEFINITIONS>)

target_compile_definitions(magda_daw
    PUBLIC
    JUCE_PLUGINHOST_VST3=1
    JUCE_PLUGINHOST_AU=1
    JUCE_PLUGINHOST_LV2=1
    JUCE_MODAL_LOOPS_PERMITTED=1
    TRACKTION_ENABLE_TIMESTRETCH_SOUNDTOUCH=1
    TRACKTION_ENABLE_TIMESTRETCH_SIGNALSMITH=1
    MAGDA_DAWPROJECT_SCHEMA_DIR="${CMAKE_SOURCE_DIR}/third_party/dawproject"
    $<$<BOOL:${JUCE_ASIO_ENABLED}>:JUCE_ASIO=1>
    PRIVATE
    # Tracktion Engine carries a MAGDA test hook (magdaTestEvaluateLoopSessionDecision,
    # in tracktion_TransportControl.cpp) gated on MAGDA_ENABLE_TEST_HOOKS. TE now
    # compiles ONCE here instead of being recompiled by magda_juce_tests, so the
    # hook must be built into magda_daw's archive when tests are enabled, or the
    # JUCE test exe fails to link it. Off for release (MAGDA_BUILD_TESTS=OFF), so
    # the hook is never shipped.
    $<$<BOOL:${MAGDA_BUILD_TESTS}>:MAGDA_ENABLE_TEST_HOOKS=1>
    # Route jassert through juce::Logger rather than straight to stderr, so a
    # test can read the engine's own assertions as a result instead of as
    # console noise. It has to be set here because this is where Tracktion is
    # compiled, and an assertion's logging is decided where the macro expands,
    # not where the test that wants to see it lives.
    #
    # What it buys: an assertion is not fatal, so a graph Tracktion objects to
    # renders anyway and produces a buffer that compares perfectly well. The
    # null-diff runner watches per case and refuses to certify one that
    # provoked an assertion. Test builds only, on the same gate as the hook
    # above, so nothing about a shipping build changes.
    $<$<BOOL:${MAGDA_BUILD_TESTS}>:JUCE_LOG_ASSERTIONS=1>
)

target_link_libraries(magda_daw_app
    PRIVATE
    magda_daw
    magda_agents
    magda_scripting
    MagdaAssets
    # JUCE/TE come transitively from magda_daw: its re-exported INTERFACE gives
    # the headers + defines to compile this target's own sources, and its
    # $<LINK_ONLY:...> forwards the modules' frameworks + the objects already in
    # magda_daw's archive. Linking the juce:: modules here directly would just
    # recompile the whole stack again (the duplication this refactor removes).
)

target_compile_definitions(magda_daw_app
    PRIVATE
    JUCE_PLUGINHOST_VST3=1
    JUCE_PLUGINHOST_AU=1
    JUCE_PLUGINHOST_LV2=1
    TRACKTION_ENABLE_TIMESTRETCH_SOUNDTOUCH=1
    TRACKTION_ENABLE_TIMESTRETCH_SIGNALSMITH=1
    $<$<BOOL:${JUCE_ASIO_ENABLED}>:JUCE_ASIO=1>
)

if(APPLE)
    # Do not let hosted plugins bind to MAGDA's statically linked JUCE symbols.
    target_link_options(magda_daw_app PRIVATE "-Wl,-exported_symbol,_main")
endif()

if(JUCE_ASIO_ENABLED)
    target_include_directories(magda_daw PUBLIC
        "${ASIO_SDK_DIR}/common"
        "${ASIO_SDK_DIR}/host"
    )
    target_include_directories(magda_daw_app PRIVATE
        "${ASIO_SDK_DIR}/common"
        "${ASIO_SDK_DIR}/host"
    )
endif()

target_include_directories(magda_daw
    PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/core
    ${CMAKE_CURRENT_SOURCE_DIR}/interfaces
    ${CMAKE_CURRENT_SOURCE_DIR}/audio
    ${CMAKE_CURRENT_SOURCE_DIR}/engine
    ${CMAKE_CURRENT_SOURCE_DIR}/project
    ${CMAKE_CURRENT_SOURCE_DIR}/profiling
    ${CMAKE_CURRENT_BINARY_DIR}/generated
    PRIVATE
    ${CMAKE_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/third_party/faust/architecture
)

target_include_directories(magda_daw_app
    PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/core
    ${CMAKE_CURRENT_SOURCE_DIR}/audio
    ${CMAKE_CURRENT_SOURCE_DIR}/ui
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/themes
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/code
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/windows
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/dialogs
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/panels
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/panels/state
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/panels/content
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/views
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/layout
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/state
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/common
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/timeline
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/tracks
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/clips
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/pianoroll
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/waveform
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/chain
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/automation
    ${CMAKE_CURRENT_SOURCE_DIR}/ui/components/common/curve
    ${CMAKE_SOURCE_DIR}
)

target_link_libraries(magda_plugin_scanner
    PRIVATE
    juce::juce_core
    juce::juce_events
    juce::juce_audio_basics
    juce::juce_audio_processors
    $<$<PLATFORM_ID:Linux>:juce::pkgconfig_JUCE_CURL_LINUX_DEPS>
)

target_compile_definitions(magda_plugin_scanner
    PRIVATE
    JUCE_PLUGINHOST_VST3=1
    JUCE_PLUGINHOST_AU=1
    JUCE_PLUGINHOST_LV2=1
    JUCE_WEB_BROWSER=0
)

if(APPLE)
    # Keep scanner exports aligned with the host app.
    target_link_options(magda_plugin_scanner PRIVATE "-Wl,-exported_symbol,_main")
    target_link_libraries(magda_plugin_scanner
        PRIVATE
        "-framework AudioUnit"
        "-framework CoreAudioKit"
    )
endif()

target_include_directories(magda_plugin_scanner
    PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/engine
    ${CMAKE_CURRENT_BINARY_DIR}/generated
)

target_link_libraries(magda_cli
    PRIVATE
    # Reuse the exact compiled engine just like the GUI app. Direct module
    # links would rebuild the whole JUCE/Tracktion stack inside this executable.
    magda_daw
    $<$<PLATFORM_ID:Linux>:juce::pkgconfig_JUCE_CURL_LINUX_DEPS>
)

target_compile_definitions(magda_cli
    PRIVATE
    JUCE_PLUGINHOST_VST3=1
    JUCE_PLUGINHOST_AU=1
    JUCE_PLUGINHOST_LV2=1
    TRACKTION_ENABLE_TIMESTRETCH_SOUNDTOUCH=1
    TRACKTION_ENABLE_TIMESTRETCH_SIGNALSMITH=1
    JUCE_WEB_BROWSER=0
    $<$<BOOL:${JUCE_ASIO_ENABLED}>:JUCE_ASIO=1>
)

target_include_directories(magda_cli
    PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/engine
    ${CMAKE_CURRENT_SOURCE_DIR}/project
    ${CMAKE_CURRENT_BINARY_DIR}/generated
)

if(APPLE)
    target_link_options(magda_cli PRIVATE "-Wl,-exported_symbol,_main")
endif()

# On MSVC Release builds, generate a PDB for crash symbolization.
# /Zi is applied here (not globally) so llama.cpp and other third-party targets
# are not affected and don't hit parallel PDB write conflicts when using sccache.
if(MSVC)
    target_compile_options(magda_daw_app PRIVATE $<$<CONFIG:Release>:/Zi>)
    target_link_options(magda_daw_app PRIVATE $<$<CONFIG:Release>:/DEBUG /OPT:REF /OPT:ICF>)
endif()

install(TARGETS magda_daw_app DESTINATION bin)

# =============================================================================
# Post-build resource copies
# =============================================================================

# Copy the plugin scanner executable next to the main app.
if(APPLE)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy
            "$<TARGET_FILE:magda_plugin_scanner>"
            "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/MacOS/magda_plugin_scanner"
        COMMENT "Copying plugin scanner to app bundle"
    )
elseif(WIN32)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy
            "$<TARGET_FILE:magda_plugin_scanner>"
            "$<TARGET_FILE_DIR:magda_daw_app>/magda_plugin_scanner.exe"
        COMMENT "Copying plugin scanner alongside app executable"
    )
else()
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy
            "$<TARGET_FILE:magda_plugin_scanner>"
            "$<TARGET_FILE_DIR:magda_daw_app>/magda_plugin_scanner"
        COMMENT "Copying plugin scanner alongside app executable"
    )
endif()

# Stage the MCP stdio bridge next to the main app (issue #1858).
#
# It has to be somewhere a user can name in an MCP host's config, and beside the
# executable is the one place that is true of every install layout: inside the
# bundle on macOS, in the install directory on Windows, next to the binary on
# Linux. The AI settings page composes the config snippet from exactly this path,
# so a dev build and a shipped one both produce a snippet that works.
#
# A separate always-run target rather than a POST_BUILD on magda_daw_app. A
# POST_BUILD only fires when its own target relinks, so a change to the bridge
# alone left a stale copy inside the bundle while the freshly built one sat in
# its own artefacts directory — the build reported success and the installed
# binary was the old one. `copy_if_different` makes running every build free
# when nothing moved.
if(APPLE)
    set(MAGDA_MCP_BRIDGE_DEST "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/MacOS/magda-mcp")
elseif(WIN32)
    set(MAGDA_MCP_BRIDGE_DEST "$<TARGET_FILE_DIR:magda_daw_app>/magda-mcp.exe")
else()
    set(MAGDA_MCP_BRIDGE_DEST "$<TARGET_FILE_DIR:magda_daw_app>/magda-mcp")
endif()

add_custom_target(magda_stage_mcp_bridge ALL
    COMMAND ${CMAKE_COMMAND} -E copy_if_different
        "$<TARGET_FILE:magda_mcp_bridge>"
        "${MAGDA_MCP_BRIDGE_DEST}"
    # Both, and in this order: the bridge supplies the file, and the app has to
    # have created the bundle directory it is copied into.
    DEPENDS magda_mcp_bridge magda_daw_app
    COMMENT "Staging MCP bridge beside MAGDA"
    VERBATIM
)

# Copy lang/ (localization JSON) into the app so StringTable can load it at runtime.
set(MAGDA_LANG_SRC "${CMAKE_CURRENT_SOURCE_DIR}/../../lang")
if(APPLE)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_LANG_SRC}"
            "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/Resources/lang"
        COMMENT "Copying localization files to app bundle Resources"
    )
else()
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_LANG_SRC}"
            "$<TARGET_FILE_DIR:magda_daw_app>/lang"
        COMMENT "Copying localization files alongside app executable"
    )
endif()

# Copy resources/controllers/ (hardware profile JSON) into the app so
# ControllerProfileRegistry can load profiles at runtime.
set(MAGDA_CONTROLLERS_SRC "${CMAKE_CURRENT_SOURCE_DIR}/../../resources/controllers")
if(APPLE)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_CONTROLLERS_SRC}"
            "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/Resources/controllers"
        COMMENT "Copying controller profiles to app bundle Resources"
    )
else()
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_CONTROLLERS_SRC}"
            "$<TARGET_FILE_DIR:magda_daw_app>/controllers"
        COMMENT "Copying controller profiles alongside app executable"
    )
endif()

# Copy resources/drumkits/ (stock .mdgk drumkit templates) into the app so
# DrumkitManager can seed the user's Drumkits folder on first launch.
set(MAGDA_DRUMKITS_SRC "${CMAKE_CURRENT_SOURCE_DIR}/../../resources/drumkits")
if(APPLE)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_DRUMKITS_SRC}"
            "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/Resources/drumkits"
        COMMENT "Copying stock drumkits to app bundle Resources"
    )
else()
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_DRUMKITS_SRC}"
            "$<TARGET_FILE_DIR:magda_daw_app>/drumkits"
        COMMENT "Copying stock drumkits alongside app executable"
    )
endif()

# Copy resources/models/ (the bundled audio-to-MIDI model, basic_pitch.onnx)
# into the app so TranscriptionService::findBundledModel resolves it at runtime.
# Single small file (~230 KB); always staged regardless of MAGDA_HAVE_CLAP so
# the dev-tree fallback and packaging stay uniform.
set(MAGDA_MODELS_SRC "${CMAKE_CURRENT_SOURCE_DIR}/../../resources/models")
if(APPLE)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_MODELS_SRC}"
            "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/Resources/models"
        COMMENT "Copying bundled models to app bundle Resources"
    )
else()
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_MODELS_SRC}"
            "$<TARGET_FILE_DIR:magda_daw_app>/models"
        COMMENT "Copying bundled models alongside app executable"
    )
endif()

# Runtime-compiled devices need the Faust standard library beside every host.
magda_stage_faust_libraries(magda_daw_app)
magda_stage_faust_libraries(magda_cli)
magda_stage_faust_runtime_dsp(magda_daw_app)
magda_stage_faust_runtime_dsp(magda_cli)

# Copy third_party/dawproject/ (vendored Project.xsd + MetaData.xsd) into the
# app so DawProjectValidator can validate import/export against the schemas at
# runtime. Without this, the build-time MAGDA_DAWPROJECT_SCHEMA_DIR source path
# does not exist on an end-user machine and every validate call fails.
set(MAGDA_DAWPROJECT_SCHEMA_SRC "${CMAKE_SOURCE_DIR}/third_party/dawproject")
if(APPLE)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_DAWPROJECT_SCHEMA_SRC}"
            "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/Resources/dawproject"
        COMMENT "Copying DAWproject schemas to app bundle Resources"
    )
else()
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
            "${MAGDA_DAWPROJECT_SCHEMA_SRC}"
            "$<TARGET_FILE_DIR:magda_daw_app>/dawproject"
        COMMENT "Copying DAWproject schemas alongside app executable"
    )
endif()

# Bundle the ONNX Runtime shared library with the app. The linker only bakes
# the FetchContent `_deps/onnxruntime-src/lib/` build-dir path into the binary's
# rpath, which does not exist on an end-user machine, so the distributed app
# must carry the runtime library itself or it aborts at launch ("Library not
# loaded: @rpath/libonnxruntime.1.dylib"). Skipped when MAGDA_HAVE_CLAP is off
# (Intel macOS) since there's no ORT to bundle.
if(MAGDA_HAVE_CLAP)
    if(APPLE)
        # Place the dylib in the bundle's Frameworks/ — the release codesign step
        # already signs everything there — and add an rpath so the loader finds it.
        # The executable references @rpath/libonnxruntime.1.dylib (the lib's own
        # install name), so the bundled file must use that exact name.
        add_custom_command(TARGET magda_daw_app POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E make_directory
                "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/Frameworks"
            COMMAND ${CMAKE_COMMAND} -E copy
                "${ONNXRUNTIME_LIB_DIR}/libonnxruntime.dylib"
                "$<TARGET_BUNDLE_CONTENT_DIR:magda_daw_app>/Frameworks/libonnxruntime.1.dylib"
            COMMENT "Bundling ONNX Runtime dylib into app Frameworks"
        )
        set_property(TARGET magda_daw_app APPEND PROPERTY
            BUILD_RPATH "@executable_path/../Frameworks")
    elseif(WIN32)
        # Windows only searches the exe's directory, the cwd, and PATH — none of
        # which include the FetchContent lib dir. onnxruntime.dll is a regular
        # load-time import of MAGDA.exe, so omitting it prevents the application
        # from reaching main().
        add_custom_command(TARGET magda_daw_app POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_if_different
                "${ONNXRUNTIME_LIB_DIR}/onnxruntime.dll"
                "${ONNXRUNTIME_LIB_DIR}/onnxruntime_providers_shared.dll"
                "$<TARGET_FILE_DIR:magda_daw_app>"
            COMMENT "Copying ONNX Runtime DLLs alongside app executable"
        )
    endif()
endif()

# libxml2 (DAWproject XSD validation) comes from vcpkg on Windows and links as a
# DLL. Windows only searches the exe's own directory, so copy libxml2.dll plus
# its transitive runtime deps next to the executable. TARGET_RUNTIME_DLLS
# resolves the full closure from the imported vcpkg targets, so we don't have to
# track the exact dep set (iconv/lzma/zlib/...) by hand. Unconditional on
# Windows: libxml2 is always linked (unlike ONNX, which is gated on CLAP).
if(WIN32)
    add_custom_command(TARGET magda_daw_app POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "$<TARGET_RUNTIME_DLLS:magda_daw_app>"
            "$<TARGET_FILE_DIR:magda_daw_app>"
        COMMAND_EXPAND_LISTS
        COMMENT "Copying runtime DLLs (libxml2 + deps) alongside app executable"
    )
endif()

# SUNROOM shares the real composer with the offline renderer.
target_sources(magda_cli PRIVATE sunroom/SunroomActions.cpp)
target_compile_definitions(magda_cli PRIVATE SUNROOM_SOURCE_DIR="${PROJECT_SOURCE_DIR}")

if(CMAKE_CXX_COMPILER_ID MATCHES "Clang|GNU")
    target_precompile_headers(magda_daw_app PRIVATE
        <juce_gui_basics/juce_gui_basics.h>
        <tracktion_engine/tracktion_engine.h>)
endif()

# JUCE's ListenerList changes layout when assertion logging is enabled. Device
# packs and the engine exchange JUCE/Tracktion objects, so they MUST agree on
# that setting (otherwise even Plugin's multiple-inheritance thunks differ).
# Keep this on the shared SDK/value targets, not the independent scanner process.
target_compile_definitions(magda_device_sdk INTERFACE
    $<$<BOOL:${MAGDA_BUILD_TESTS}>:JUCE_LOG_ASSERTIONS=1>)
target_compile_definitions(magda_types PUBLIC
    $<$<BOOL:${MAGDA_BUILD_TESTS}>:JUCE_LOG_ASSERTIONS=1>)
target_compile_definitions(magda_music PUBLIC
    $<$<BOOL:${MAGDA_BUILD_TESTS}>:JUCE_LOG_ASSERTIONS=1>)
if(CMAKE_CXX_COMPILER_ID MATCHES "Clang|GNU")
    target_precompile_headers(magda_tracktion_compat_devices PRIVATE
        <tracktion_engine/tracktion_engine.h>)
endif()
